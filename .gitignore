node_modules
dist
.env
.env.*
!.env.example

# Local only (not for the public repo)
pre_launch_checklist.md
code-review-order.md

# Cursor IDE (local only — not published to the repo)
.cursor/

# Logs & OS noise
*.log
npm-debug.log*
.DS_Store
Thumbs.db